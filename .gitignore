node_modules
dist
.DS_Store
*.local
tsconfig.tsbuildinfo

# External data corpora (imported as needed)
BunksLua/

# Original artifact archive (provenance kept via docs)
ClaudeChat2Code.zip

# Sample MIDI files (user test data, not source)
ChordSequences/
*.mid
# But allow preloaded samples shipped with the app
!public/samples/*.mid

# Debug export artifacts (generated by "Export Chord Debug" button)
*_chord-debug*.json

# Audio files (test data, not source code)
*.aif
*.aiff
*.caf

# Database files
*.db

# Analysis output files
analysis*.txt
